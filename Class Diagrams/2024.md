```mermaid
classDiagram
    direction TB

    class ITextFormatter {
        <<interface>>
        +txtFormat(text: string): string
    }

    class UppercaseFormatter {
        +txtFormat(text: string): string
    }

    class LowercaseFormatter {
        +txtFormat(text: string): string
    }

    class TextEditor {
        -formatter: ITextFormatter
        +setFormatter(formatter: ITextFormatter)
        +publishText(text: string): string
    }

    ITextFormatter <|.. UppercaseFormatter
    ITextFormatter <|.. LowercaseFormatter
    TextEditor o-- ITextFormatter : uses
```